export enum VaultType {
    Locked,
    Liquid
}

export enum EndowmentType {
    Charity,
    Normal
}

export enum StrategyApprovalState {
    NOT_APPROVED,
    APPROVED,
    WITHDRAW_ONLY,
    DEPRECATED
}